{
   # plugins that should be loaded
   plugins => [
      # basic plugins
      "User",
      "Group",
      "Permission",
   ],

   # database configuration
   # currently only mysql is supported
   database => {
      host     => "127.0.0.1",
      schema   => "rexio",
      username => "rexio",
      password => "rexio",
   },

   # see https://metacpan.org/module/Digest::Bcrypt for more information
   auth => {
      salt => 'ywpqmiwdetgcpmle', # 16 bytes long
      cost => 1, # between 1 and 31
   },

   # session settings
   session => {
      key => 'Rex.IO.Server',
   },

   log => {
      file => "-",
      level => "debug",
   },
}
